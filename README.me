CurrencyAPI Project

A project that displays a list of currencies where the user can retrieve its data via REST API.

Functions:

Add: Adding a specific currency via REST API. A response will be shown if successful or not.
/add/[Currency] 

Example: CurrencyAPI/add/USD

Update: Updates all currencies rates. A response will be shown if successful or not.
/update

Example: CurrencyAPI/update

